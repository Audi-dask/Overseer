# 复制为 .env 后修改：cp .env.example .env
# MASTER_KEY 用于加密 GitLab Token / LLM API Key，变更后需重新填写凭证
MASTER_KEY=change-me-use-openssl-rand-hex-32

# 可选：预置管理员密码，跳过首次设置页
# ADMIN_PASSWORD=your-admin-password

# 宿主机映射端口
PORT=8080
